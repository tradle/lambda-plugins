{
  "name": "@lambda-plugins/main",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "preinstall": "cd ..; npm run prepare >/dev/null; npm pack --ignore-scripts --loglevel=error"
  },
  "devDependencies": {
    "@types/debug": "^4.1.7",
    "serverless": "^3.15.0",
    "serverless-plugin-typescript": "^2.1.2",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@tradle/lambda-plugins": "file:../tradle-lambda-plugins-3.0.1.tgz",
    "debug": "^4.3.4"
  }
}
