{
  "name": "@lambda-plugins/main",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "postinstall": "npm i --no-save ../$(cd ..; npm pack --ignore-scripts --loglevel=error)"
  },
  "devDependencies": {
    "@types/debug": "^4.1.7",
    "serverless": "^3.1.1",
    "serverless-plugin-typescript": "^2.1.1",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "debug": "^4.3.3"
  }
}
